import React from "react";
import CreateAccessToken from "@/components/setting/token-new";

const page = () => {
  return (
    <div>
      <CreateAccessToken />
    </div>
  );
};

export default page;
